import * as React from "react";
import icon from "./icons/file-submodule.svg"
import {Icon} from "./Icon";

export const FileSubmodule = (props) => {
    return <Icon ratio={14.0/16} src={icon} {...props} />
};

