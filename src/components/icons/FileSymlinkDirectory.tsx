import * as React from "react";
import icon from "./icons/file-symlink-directory.svg"
import {Icon} from "./Icon";

export const FileSymlinkDirectory = (props) => {
    return <Icon ratio={12.0/16} src={icon} {...props} />
};

