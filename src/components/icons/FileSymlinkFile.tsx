import * as React from "react";
import icon from "./icons/file-symlink-file.svg"
import {Icon} from "./Icon";

export const FileSymlinkFile = (props) => {
    return <Icon ratio={12.0/16} src={icon} {...props} />
};

