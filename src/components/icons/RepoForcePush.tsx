import * as React from "react";
import icon from "./icons/repo-force-push.svg"
import {Icon} from "./Icon";

export const RepoForcePush = (props) => {
    return <Icon ratio={12.0/16} src={icon} {...props} />
};

