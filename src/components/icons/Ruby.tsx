import * as React from "react";
import icon from "./icons/ruby.svg"
import {Icon} from "./Icon";

export const Ruby = (props) => {
    const {style} = props;
    return <Icon source={icon} style={style} />
};

