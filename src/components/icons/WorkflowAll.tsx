import * as React from "react";
import icon from "./icons/workflow-all.svg"
import {Icon} from "./Icon";

export const WorkflowAll = (props) => {
    const {style} = props;
    return <Icon source={icon} style={style} />
};

