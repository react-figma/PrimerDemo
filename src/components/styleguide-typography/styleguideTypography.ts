import {StyleSheet} from "react-figma";

export const styleguideTypography = StyleSheet.create({
    label: {
        fontFamily: "SF Pro Display",
        fontSize: 16,
        color: "#afafaf"
    }
});
